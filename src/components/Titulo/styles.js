import styled from 'styled-components'

export const FonteTitulo = styled.h2`
        color: var(--cor-menu);
        font-family: var(--fonte-menu);
        font-style: normal;
        font-weight: 900;
`


